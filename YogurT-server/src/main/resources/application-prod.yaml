server:
  port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yogurt_prod?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: yogurt_api_prod
    password: prod123
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 600000

  flyway:
    enabled: true
    baseline-on-migrate: false
    locations: classpath:db/migration
    user: yogurt_flyway
    password: flyway123